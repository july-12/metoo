# name: 'test'
# description: 'run action test'
# inputs:
#   test_token:
#     description: 'token'
#     # default: ${{ test_token }}
#     required: true
#   other_token:
#     default: "hao12"
# runs:
#   using: 'node16'
#   main: 'test.js'
#   env:
#     test_token: ${{ inputs.test_token }}
name: 'test'

description: 'test'

inputs:
  token:
    description: 'A Github PAT'
    default: "haha"
    required: true

runs:
  using: 'composite'
  steps:
    - name: Create Branch
      using: 'node16'
      main: 'test.js'
      env:
        test_token: ${{ inputs.token }}
    
